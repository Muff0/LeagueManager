using Microsoft.AspNetCore.SignalR;

namespace Shared.Notifications;
public class NotificationHub : Hub
{
    // No methods needed — server pushes, clients just listen.
    // Add auth here later if needed.
}